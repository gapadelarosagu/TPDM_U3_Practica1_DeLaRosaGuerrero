<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.edu.ittepic.tpdm_u3_practica1_delarosaguerrerogabriela">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Principal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".InsAlu" />
        <activity android:name=".ModAlu" />
        <activity android:name=".LisAlu" />
        <activity android:name=".InsDoc" />
        <activity android:name=".ModDoc" />
        <activity android:name=".LisDoc"></activity>
    </application>

</manifest>